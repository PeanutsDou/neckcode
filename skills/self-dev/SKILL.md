---
name: self-dev
description: 通用项目开发流程管理。TRIGGER when: 用户对任何项目提出开发需求、新功能、改进、Bug 修复。管理项目 spec 目录下的开发文档，确保开发前文档先行、开发后文档同步。
version: 0.1.0
---

# Self-Dev — 通用开发流程管理

适用于所有项目的标准化开发流程。**核心原则：文档先行，确认后开发，完成后同步。**

## 核心规则

**绝对禁止在开发文档未经用户确认的情况下直接进行开发落地。**

## 开发状态

| 状态 | 含义 |
|------|------|
| 规划中 | 需求已记录，方案讨论中，待用户确认 |
| 执行中 | 方案已确认，开发进行中 |
| 已完成 | 开发完成并验证，待归档 |

## 文档命名规范

```
<项目根目录>/spec/<功能子目录>/<功能>-<状态>-<日期>.md
```

- 状态使用英文：planning / in-progress / completed
- 日期格式：YYYYMMDD

示例：
- `spec/agent-panel/agent-panel-planning-20260514.md`
- `spec/ui-refactor/sidebar-in-progress-20260520.md`
- `spec/bug-fix/memory-leak-completed-20260510.md`

## 工作流程

### Step 1：接收需求

```
a. 检查 spec/ 目录下是否存在相关子目录和文档
b. 如存在相关文档 → 读取并告知用户现有内容，询问是否继续
c. 如不存在 → 创建子目录和规划文档，子目录名以需求关键词命名
d. 文档初始状态设为「规划中」
e. 如果用户未明确子目录命名，先创建结构再找用户确认
```

### Step 2：需求细化

```
a. 在文档中详细记录需求描述、技术方案、影响范围、风险点
b. 用户确认方案后 → 状态改为「执行中」
c. 仅在用户明确说"开始开发"后才进入开发
```

### Step 3：开发执行

```
a. 严格按文档中确认的方案执行
b. 如有偏离，先更新文档再继续
c. 阶段性成果及时更新到文档
```

### Step 4：开发完成

```
a. 状态改为「已完成」，更新完成日期
b. 将完成项同步到 spec/now-function-completed.md（如该项目有此文件）
c. 提交代码 + 文档
```

## spec 目录结构示例

```
spec/
├── now-function-completed.md        # 已完成功能总览
├── agent-panel/                     # Agent 面板功能
│   └── agent-panel-planning-20260514.md
├── ui-refactor/                     # UI 重构
├── bug-fix/                         # Bug 修复
└── ...
```

## 重要提醒

- 文档是开发的唯一依据，口头需求也必须先落地为文档
- 每次完成开发后**立即更新文档**，确保反映真实进度
- spec 目录纳入 Git 版本管理，与代码同步提交
